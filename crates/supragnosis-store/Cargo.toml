[package]
name = "supragnosis-store"
description = "supragnosis store adapters: in-memory, Cozo(RocksDB), redb."
version.workspace = true
edition.workspace = true
license.workspace = true

[lints]
workspace = true

[dependencies]
supragnosis-core.workspace = true
cozo.workspace = true
redb.workspace = true
serde_json.workspace = true
# DeserializeOwned bound on the redb adapter's generic row loader.
serde.workspace = true
# Record enumeration degrade (excluding corrupt rows) instead of silencing it (Principle 19).
tracing.workspace = true
